apiVersion: apps/v1
kind: Deployment
metadata:
  name: flights-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: flights-api
  template:
    metadata:
      labels:
        app: flights-api
    spec:
      containers:
        - name: flights-api
          image: rubixacr.azurecr.io/kubewrkshp/flightsapi:v26023
          imagePullPolicy: Always
      imagePullSecrets:
        - name: rubixacr
apiVersion: v1
kind: Service
metadata:
  name: flights-api
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: flights-api
